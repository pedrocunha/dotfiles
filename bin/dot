#!/bin/sh
#
# dot
#
# `dot` handles installation, updates, things like that. Run it periodically
# to make sure you're on the latest and greatest.
# Upgrade homebrew
brew update
brew upgrade antigen coreutils ctags fzf go nvim rbenv ripgrep ruby-build rust zsh tmux
